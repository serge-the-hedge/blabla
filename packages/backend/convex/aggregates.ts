// Aggregate component is mounted for efficient counters once the app has enough data volume.
// The MVP computes counters from indexed queries and keeps this module as the integration point
// for follow-up aggregate-backed summaries.
export {};
